{
  "name": "zk_pic_workshop",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/aztecprotocol/devcontainer-features/noir:1": {},
    "ghcr.io/aztecprotocol/devcontainer-features/barretenberg:1": {}
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "noir-lang.vscode-noir",
        "esbenp.prettier-vscode"
      ]
    }
  },
  "postCreateCommand": "corepack enable && pnpm install && curl -L https://raw.githubusercontent.com/noir-lang/noirup/main/install | bash && curl -L https://raw.githubusercontent.com/AztecProtocol/aztec-packages/master/barretenberg/cpp/installation/install | bash && ~/.noirup/bin/noirup --version 1.0.0-beta.18 && ~/.noirup/bin/noirup --version && ~/.bbup/bin/bbup && ~/.bbup/bin/bb --version",
  "remoteUser": "node"
}
